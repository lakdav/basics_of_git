# checkout

```bash
# Объединение изменений в другую ветку
git checkout -m main

# Создание новой ветки и переключение на нее
qit checkout -Ь test


git checkout -- text.txt


git checkout -b serverfix 877h8ybh
git checkout -b serverfix origin/serverfix
```
