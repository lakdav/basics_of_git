# remote

```bash
git remote

git remote -v

# add
git remote add origin https://github.com/paulboone/ticgit

git remote add notorigin https://github.com/paulboone/ticgit


git remote show origin

# Renaming
git remote rename origin Origin

# Removing
git remote remove origin
```
